# Mode selector. The backend will load `backend-node/.env.local` or `backend-node/.env.prod`
# based on APP_MODE before validating environment variables.
# Set this in the shell before starting the server:
#   PowerShell: $env:APP_MODE='local'
#   cmd.exe:    set APP_MODE=local
APP_MODE=prod

# See `backend-node/.env.local` and `backend-node/.env.prod` for the actual mode-specific values.
