-- Run this while connected to the default `postgres` database (or any admin DB).
-- If you use the official postgres Docker image, you can skip this file by setting:
--   POSTGRES_DB=pichufy

CREATE DATABASE pichufy;

